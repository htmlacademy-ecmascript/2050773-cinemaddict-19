const FilterType = {
  WATCHLIST: 'Watchlist',
  HISTORY: 'history',
  FAVORITES: 'favorites',
};

const SortType = {
  DEFAULT: 'default',
  DATE: 'date',
  RATING: 'rating',
};

const EMOJIS = ['smile', 'sleeping', 'puke', 'angry'];

export { FilterType, SortType, EMOJIS };
